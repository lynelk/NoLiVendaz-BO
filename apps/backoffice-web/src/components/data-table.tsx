import Link from "next/link";
export interface Column{key:string;label:string;linkPrefix?:string;}
function display(value:unknown,key:string){if(value===null||value===undefined||value==="")return"—";if(Array.isArray(value))return value.join(", ");if(typeof value==="object")return JSON.stringify(value);if(/(_at|At|date|Date)$/.test(key)){const d=new Date(String(value));if(!Number.isNaN(d.getTime()))return d.toLocaleString("en-UG",{timeZone:"Africa/Kampala"});}return String(value);}
export function DataTable({rows,columns,empty="No records found."}:{rows:Array<Record<string,unknown>>;columns:Column[];empty?:string}){if(rows.length===0)return <div className="card empty">{empty}</div>;return <div className="table-wrap"><table><thead><tr>{columns.map(c=><th key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={String(r.id??i)}>{columns.map(c=>{const v=r[c.key];return <td key={c.key}>{c.linkPrefix&&r.id?<Link className="link" href={`${c.linkPrefix}${r.id}`}>{display(v,c.key)}</Link>:display(v,c.key)}</td>})}</tr>)}</tbody></table></div>}
