export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string;next?:string}>}){const q=await searchParams;return <div className="login-card"><div className="brand-mark login-mark">NV</div><span className="eyebrow">NOLI Vendaz</span><h1>Operator Back Office</h1><p>Sign in with the organization identity provider. Access is still controlled by NOLI tenant membership and role permissions.</p>{q.error?<div className="alert">Sign-in failed: {q.error}</div>:null}<a className="button login-button" href={`/auth/login${q.next?`?next=${encodeURIComponent(q.next)}`:""}`}>Continue to secure sign in</a><small>Production sessions are HttpOnly, short-lived and never exposed to browser JavaScript.</small></div>}
