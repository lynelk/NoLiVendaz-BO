import { apiGet } from "../../lib/api";import { DataTable } from "../../components/data-table";
export default async function Page(){const rows=await apiGet<Array<Record<string,unknown>>>("/api/v1/devices");return <><header className="page-head"><div><span className="eyebrow">Edge estate</span><h1>Devices</h1><p>Normalized vending devices and provider mappings. Remote actions remain capability-controlled and audited.</p></div></header><DataTable rows={rows} columns={[{key:"code",label:"Device"},{key:"device_type",label:"Type"},{key:"merchant_name",label:"Merchant"},{key:"site_name",label:"Site"},{key:"status",label:"Status"},{key:"firmware_version",label:"Firmware"},{key:"network_status",label:"Network"},{key:"last_seen_at",label:"Last seen"}]}/></>}
