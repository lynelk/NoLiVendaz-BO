import{apiGet}from"../../lib/api.js";import type{OperatorContext}from"../../lib/types.js";import{DataTable}from"../../components/data-table.js";import{createProductAction,createServiceAction}from"../actions.js";export default async function Page(){const[services,products,op]=await Promise.all([apiGet<Array<Record<string,unknown>>>('/api/v1/services'),apiGet<Array<Record<string,unknown>>>('/api/v1/products'),apiGet<OperatorContext>('/api/v1/auth/context')]);const can=op.isPlatformAdmin||op.permissions.includes('catalog.manage');return <><header className="page-head"><div><span className="eyebrow">Canonical catalogue</span><h1>Services & Products</h1><p>Normalized service families and product rules shared across provider adapters.</p></div></header>{can?<div className="grid two-col"><section className="card"><h2>Add service</h2><form action={createServiceAction} className="form-grid"><input name="code" placeholder="Service code" required/><input name="name" placeholder="Service name" required/><input name="category" placeholder="electricity / airtime / physical" required/><button className="button">Create service</button></form></section><section className="card"><h2>Add product</h2><form action={createProductAction} className="form-grid"><select name="serviceId" required><option value="">Service…</option>{services.map(s=><option key={String(s.id)} value={String(s.id)}>{String(s.name)}</option>)}</select><input name="code" placeholder="Product code" required/><input name="name" placeholder="Product name" required/><input name="currency" placeholder="UGX"/><input name="minAmount" placeholder="Min amount" inputMode="decimal"/><input name="maxAmount" placeholder="Max amount" inputMode="decimal"/><input name="fixedPrice" placeholder="Fixed price" inputMode="decimal"/><label><input type="checkbox" name="variableAmountAllowed" defaultChecked/> Variable amount allowed</label><button className="button">Create product</button></form></section></div>:null}<div className="stack"><DataTable rows={services} columns={[{key:'code',label:'Service code'},{key:'name',label:'Service'},{key:'category',label:'Category'},{key:'status',label:'Status'}]}/><DataTable rows={products} columns={[{key:'service_name',label:'Service'},{key:'internal_product_code',label:'Product code'},{key:'name',label:'Product'},{key:'currency',label:'Currency'},{key:'min_amount',label:'Min'},{key:'max_amount',label:'Max'},{key:'fixed_price',label:'Fixed price'},{key:'enabled',label:'Enabled'}]}/></div></>}
