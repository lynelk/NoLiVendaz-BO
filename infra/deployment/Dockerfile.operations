FROM node:22-alpine AS build
RUN corepack enable
WORKDIR /app
COPY . .
RUN pnpm install --no-frozen-lockfile && pnpm --filter @nolivendaz/operations-worker... build
CMD ["node","services/operations-worker/dist/worker.js"]
